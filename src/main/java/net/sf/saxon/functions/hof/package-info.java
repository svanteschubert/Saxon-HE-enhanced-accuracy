/**
 * <p>The package <code>com.saxonica.hof</code> contains code to support higher-order functions.</p>
 * <p>Higher-order functions are supported in XSLT 3.0, XPath 3.1, and XQuery 3.1, in Saxon-PE and Saxon-EE only.</p>
 * <p>The package includes code to support both the core facilities of dynamic creation and invocation of function
 * objects, and also specific higher-order functions defined in the <i>Functions and Operators</i> library
 * such as <code>fn:filter</code>, <code>fn:for-each</code> and <code>fn:fold-left</code>.</p>
 */
package net.sf.saxon.functions.hof;
